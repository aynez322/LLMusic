# ── Build-only data (NOT loaded at runtime) ──────────────────────────
# Runtime needs only: merged_dataset.parquet, genre_fingerprints,
# genre_classifier.pkl. Everything below is excluded to keep the image lean.
data/datasets/
data/training_dataset.parquet

# Dataset-building scripts (not used by the API)
scripts/

# Node deps / prior frontend build (rebuilt fresh in stage 1)
**/node_modules/
frontend/dist/

# Python / venv / caches
.venv/
venv/
__pycache__/
**/__pycache__/
*.py[cod]
.pytest_cache/

# Secrets, VCS, IDE, misc
.env
.git/
.claude/
output/
*.png
